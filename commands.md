Git commands
------------
git init
git status
git add .
git commit -m "message"
git clone <repo_url>

Linux commands
--------------
ls
cd /path/to/folder
pwd
rm file.txt
rm -rf folder
cp source destination
mv oldname newname

